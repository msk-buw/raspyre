============
Installation
============

At the command line::

    $ pip install Raspyre

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Raspyre
    $ pip install Raspyre

On another note::

    Trigger test
